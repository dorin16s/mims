<UserControl x:Class="MIMS.CancleAppointment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid  x:Name="table" HorizontalAlignment="Left" Margin="60,70,0,0" VerticalAlignment="Top" SelectionMode="Single"  ItemsSource="{Binding }" />
        <Button x:Name="btn_cancle" Content="Cancle" HorizontalAlignment="Left" Height="40" Margin="60,260,0,0" VerticalAlignment="Top" Width="95" Click="btn_cancle_Click"/>

    </Grid>
</UserControl>
